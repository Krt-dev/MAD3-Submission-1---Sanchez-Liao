name: midterm_project_sanchez_liao
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"
  flutter: ">=2.8.0 <3.0.0" # Adjusted to match the SDK constraint

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  go_router: ^14.1.4
  get_it: ^7.7.0
  form_field_validator: ^1.1.0
  hive: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.3.11

flutter:
  uses-material-design: true
